{
  "private": true,
  "name": "route",
  "version": "1.0.0",
  "description": "A URL shortening services running on Cloudflare Workers using Cloudflare KV as a store",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Simone Natalini <natalinis@outlook.com>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
